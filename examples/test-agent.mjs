#!/usr/bin/env node
/**
 * Example generic agent. Prefer `orvex demo` or `orvex run ./examples/test-agent.mjs`
 * so decisions go through the real engines.
 */
console.log('Use: orvex run -- ./examples/test-agent.mjs');
console.log('Or:  orvex demo');
