# Sessions

Each `orvex run` / `demo` creates a session id, statistics, and peak risk. Checkpoints snapshot selected project files before high-risk paths. Rollback refuses if the live file hash diverged from both current and snapshot hashes.
