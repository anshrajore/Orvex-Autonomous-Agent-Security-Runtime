# Audit and sessions

Store: `~/.orvex/{sessions,audit,checkpoints,policies,cache,config}`. Commands: `orvex session list|show|history|replay|export`, `orvex audit list|export`. SARIF is supported. No telemetry.
