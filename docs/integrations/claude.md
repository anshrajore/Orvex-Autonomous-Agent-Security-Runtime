# Claude Code

```bash
orvex run claude -- --dangerously-skip-permissions
```

Dangerous Claude flags still reach the child. Orvex policy remains authoritative for evaluated actions and for OS sandbox limits.
